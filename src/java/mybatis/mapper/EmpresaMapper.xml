<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="empresa">
    <insert id="registrar" parameterType="map" >
        INSERT INTO empresa(nombreComercial, nombreRepresentanteLegal, email, calle, numero, codigoPostal, ciudad, telefono, paginaWeb, RFC) VALUES (#{nombreComercial}, #{nombreRepresentanteLegal}, #{email}, #{calle}, #{numero}, #{codigoPostal}, #{ciudad}, #{telefono}, #{paginaWeb}, #{RFC});
    </insert>
</mapper>
